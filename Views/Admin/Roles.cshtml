@model List<Role>
<div class="row">
        @await Html.PartialAsync("LeftAdminMenu")
    <div class="col-9">
        <table class="table table-hover">
            <thead>
                <tr>
                    <th colspan="2">Список ролей</th>
                </tr>
            </thead>
            @foreach (var role in Model)
            {
                 <tbody class="table-group-divider">
                <tr>
                    <td class="w-75">@role.Name</td>
                    <td><a href="/admin/removeRole?roleId=@role.Id">Удалить</a></td>
                </tr>
            </tbody>
            }
           
            <tfoot>
                <tr>
                   <th colspan="1"></th>
                   <th><a href="/admin/addRole">Добавить</a></th>
                </tr>
            </tfoot>
        </table>
    </div>
</div>
